---
# This collection is initialized by https://github.com/ansible/ansible-creator 24.12.1

# See https://docs.ansible.com/ansible/latest/dev_guide/collections_galaxy_meta.html

namespace: "branic"
name: "system_management"
version: 1.1.0
readme: README.md
authors:
  - Brant Evans <bevans@redhat.com>

description: Fedora Linux system configuration and management
license_file: LICENSE
tags: ["linux", "tools", "fedora"]
dependencies:
  "ansible.posix": ">=1.5.0"
  "community.general": ">=8.0.0"

repository: https://github.com/branic/system_management
# documentation: http://docs.example.com
# homepage: http://example.com
issues: https://github.com/branic/system_management/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered. Mutually exclusive with 'manifest'
build_ignore:
  - .ansible
  - .ansible-lint
  - .devcontainer
  - .flake8
  - .github
  - .gitignore
  - .isort.cfg
  - .markdownlint-cli2.jsonc
  - .markdownlint.yml
  - .pre-commit-config.yaml
  - .prettierignore
  - .vscode
  - .yamllint.yml
  - changelogs/.plugin-cache.yaml
  - changelogs/config.yaml
  - changelogs/fragments
  - CODE_OF_CONDUCT.md
  - collections
  - CONTRIBUTING
  - devfile.yaml
  - extensions/molecule
  - MAINTAINERS
  - pyproject.toml
  - RELEASING.md
  - tox-ansible.ini
# A dict controlling use of manifest directives used in building the collection artifact. The key 'directives' is a
# list of MANIFEST.in style
# L(directives,https://packaging.python.org/en/latest/guides/using-manifest-in/#manifest-in-commands). The key
# 'omit_default_directives' is a boolean that controls whether the default directives are used. Mutually exclusive
# with 'build_ignore'
# manifest: null
