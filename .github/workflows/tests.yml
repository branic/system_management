---
name: "CI"

permissions:
  contents: read

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

on:  # yamllint disable-line rule:truthy
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  changelog:
    uses: ansible/ansible-content-actions/.github/workflows/changelog.yaml@17784d5248b78835d2f2488211367e8de5a95381
    if: github.event_name == 'pull_request'
  build-import:
    uses: ansible/ansible-content-actions/.github/workflows/build_import.yaml@17784d5248b78835d2f2488211367e8de5a95381
  ansible-lint:
    uses: ansible/ansible-content-actions/.github/workflows/ansible_lint.yaml@17784d5248b78835d2f2488211367e8de5a95381
  sanity:
    uses: ansible/ansible-content-actions/.github/workflows/sanity.yaml@05894f1a7922836072b5a12e80f9c2ed5f489567
  unit-galaxy:
    uses: ansible/ansible-content-actions/.github/workflows/unit.yaml@17784d5248b78835d2f2488211367e8de5a95381
  unit-source:
    uses: ansible-network/github_actions/.github/workflows/unit_source.yml@446d4586bcde8bf6e1a9c085983bf172b59bd7b2
    with:
      collection_pre_install: >-
        git+https://github.com/ansible-collections/ansible.utils.git
  all_green:
    # Run after every needed job finishes (success or failure), but skip when the
    # workflow run was cancelled (e.g. superseded by concurrency) so we never "pass" on cancelled deps.
    if: ${{ always() && !cancelled() }}
    needs:
      - changelog
      - build-import
      - sanity
      - unit-galaxy
      - unit-source
      - ansible-lint
    runs-on: ubuntu-latest
    steps:
      - name: Verify all required jobs succeeded
        env:
          R_CHANGELOG: ${{ needs['changelog'].result }}
          R_BUILD_IMPORT: ${{ needs['build-import'].result }}
          R_ANSIBLE_LINT: ${{ needs['ansible-lint'].result }}
          R_SANITY: ${{ needs['sanity'].result }}
          R_UNIT_GALAXY: ${{ needs['unit-galaxy'].result }}
          R_UNIT_SOURCE: ${{ needs['unit-source'].result }}
        run: |
          import os

          results = {
              "changelog": os.environ["R_CHANGELOG"],
              "build-import": os.environ["R_BUILD_IMPORT"],
              "ansible-lint": os.environ["R_ANSIBLE_LINT"],
              "sanity": os.environ["R_SANITY"],
              "unit-galaxy": os.environ["R_UNIT_GALAXY"],
              "unit-source": os.environ["R_UNIT_SOURCE"],
          }
          must_succeed = (
              "build-import",
              "ansible-lint",
              "sanity",
              "unit-galaxy",
              "unit-source",
          )
          for name in must_succeed:
              r = results[name]
              assert r == "success", f"{name}: expected success, got {r!r}"
          cl = results["changelog"]
          assert cl in ("success", "skipped"), (
              f"changelog: expected success or skipped, got {cl!r}"
          )
        shell: python
